services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anonlove-backend
    ports:
      - "8080:8080"
    environment:
      - DB_PASSWORD=${DB_PASSWORD:-anonlove123}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      # ⚠️  DEV-ONLY: SSL disabled for local development
      # For production, use: jdbc:mysql://mysql:3306/anonlove_db?useSSL=true&requireSSL=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
      # See README.md for production SSL setup
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/anonlove_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
      - SPRING_DATA_REDIS_HOST=redis
      - SPRING_DATA_REDIS_PORT=6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - anonlove-network
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: anonlove-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-anonlove123}
      MYSQL_DATABASE: anonlove_db
      TZ: Asia/Seoul
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - anonlove-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: anonlove-redis
    ports:
      - "6379:6379"
    networks:
      - anonlove-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

networks:
  anonlove-network:
    driver: bridge

volumes:
  mysql-data:
